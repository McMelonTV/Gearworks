# Gearworks

<sub><sup>Uh yeah, I sure hope it does!!</sup></sub>

## Hi Mel!
Any new folder in src/ will get its own collection
If you want easy access to that folder from anywhere you can just do: `import "<folder_name>:<package>"` Neat, huh?
Oh and you can also use `"assets:<file_path>"` when loading files with the `#load` directive.